{
    "version": "0.2.0",
    "configurations": [
        
        {
            "type": "java",
            "name": "Attach to Test JVM",
            "request": "attach",
            "hostName": "localhost",
            "port": 5005 // Must match the port used by Gradle
        }
    ]
}